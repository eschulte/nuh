<!DOCTYPE html>
<html>
<title>{{ title }}</title>
<meta charset="UTF-8">
<meta name="description" content="{{ brief }}">
<meta name="viewport" content="width=device-width, initial-scale=1">
<!-- Design by Eric Schulte, source at https://github.com/eschulte/nuh. -->
<link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
<link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Raleway">
<link rel="stylesheet" href="//use.fontawesome.com/releases/v5.4.0/css/all.css">
<script src="https://www.google.com/recaptcha/api.js"></script>
<style>
body,h1,h2{font-family: "Raleway", sans-serif}
body, html {height: 100%}
/* To align anchors. */
</style>
<body class="w3-light-grey">
  <div class="w3-container">
    <h1>{{ name }}</h1>
    {{ content | safe }}
  </div>
</body>
<script>
  function submit() {
    console.log("I've been called.")
    // document.getElementById("contact-form").submit();
    var xhr = new XMLHttpRequest();
    xhr.open("POST", "https://us-central1-winged-comfort-298422.cloudfunctions.net/contact-form"); 
    xhr.onload = function(event){ 
        alert("Success, server responded with: " + event.target.response); // raw response
    }; 
    // or onerror, onabort
    xhr.onerror = function(event){ 
        alert("There was an error uploading your form: " + event);
    }; 
    xhr.onabort = function(event){ 
        alert("There was an error uploading your form: " + event);
    }; 
    var formData = new FormData(document.getElementById("contact-form")); 
    xhr.send(formData);
}
</script>
</html>
